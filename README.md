# chimichan

Small demo and future extension for parsing Japanese text and integrating with Anki.

Repository layout:

- `web/` — static demo used for rapid iteration and manual testing.
- `src/` — TypeScript sources and React components for the full app.
- `src/anki` — AnkiConnect helpers.
